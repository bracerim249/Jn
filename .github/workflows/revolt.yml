name: worker

on:
  push:

jobs:
  implication:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        stir: ["rid", "opposite", "inferior", "vinegar", "ache"]
        patience: ["eliminate", "harmony", "oral", "string"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Bt
